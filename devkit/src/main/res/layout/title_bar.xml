<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/size_44dp"
    android:id="@+id/titleBarRl"
    android:background="@color/white">

    <TextView
        android:id="@+id/titleBarTvLeft"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_centerVertical="true"
        android:drawableStart="@drawable/common_icon_back"
        android:paddingStart="@dimen/size_12dp"
        android:paddingEnd="@dimen/size_12dp" />

    <TextView
        android:id="@+id/titleBarTvRight"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:paddingStart="@dimen/size_12dp"
        android:paddingEnd="@dimen/size_12dp"
        android:gravity="center"/>

    <TextView
        android:id="@+id/titleBarTv"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:text="DEFAULT"
        android:textColor="@android:color/black"
        android:textSize="@dimen/size_17sp"
        android:textStyle="bold" />

    <FrameLayout
        android:visibility="gone"
        android:id="@+id/titleBarFl"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_toStartOf="@id/titleBarTvRight"
        android:layout_toEndOf="@id/titleBarTvLeft" />

</RelativeLayout>